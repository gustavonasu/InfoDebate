<%= render 'shared/admin_title_options', :title => "Editing commenter",
 				:options => [ {:label => "Show", :path => commenter_path(@commenter), :type => "info"},
	 						  {:label => "Back", :path => commenter_path, :type => "info"} ] %>

<%= form_for(@commenter, :html => {:class => "form-horizontal"}) do |f| %>
	<%= render 'fields', :f => f %>
	<div class="controls">
	    <%= f.submit "Update", :class => "btn btn-primary btn-large" %>
	</div>
<% end %>
