<%= form_tag path, :method => 'get' do %>
  <% fields.each do |field, options| %>
    <%= send(options[:tag], field, params[field], :class => options[:class]) %>
  <% end %>
  <%= submit_tag "Buscar", :class => "btn" %>
<% end %>