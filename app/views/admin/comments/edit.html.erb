<%= render 'shared/admin_title_options', :title => "Atualização de Comentário",
           :options => [ {:label => "Voltar", :path => admin_comments_path, :type => "info"} ] %>

<%= form_for([:admin, @comment], :html => {:class => "form-horizontal"}) do |f| %>
  <%= render 'fields', :f => f %>	
  <div class="controls"> 
    <%= f.submit "Salvar", :class => "btn btn-primary btn-large" %>
  </div>
<% end %>
