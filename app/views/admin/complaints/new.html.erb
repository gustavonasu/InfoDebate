<%= render 'shared/admin_title_options', :title => "Nova Comentário",
           :options => [ {:label => "Voltar", :path => admin_complaints_path, :type => "info"} ] %>

<%= form_for([:admin, @complaint], :html => {:class => "form-horizontal"}) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="controls"> 
    <%= f.submit "Salvar", :class => "btn btn-primary btn-large" %>
  </div>
<% end %>