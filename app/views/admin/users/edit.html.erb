<%= render 'shared/admin_title_options', :title => "Editing user",
 				:options => [ {:label => "Show", :path => admin_user_path(@user), :type => "info"},
							  {:label => "Delete", :path => admin_user_path(@user), :type => "danger", 
								:method => "delete", :confirmation_msg => "Are you sure to proceed?"},
	 						  {:label => "Back", :path => admin_user_path, :type => "info"} ] %>

<%= form_for([:admin, @user], :html => {:class => "form-horizontal"}) do |f| %>
	<%= render 'fields', :f => f %>
	<div class="controls">
	    <%= f.submit "Update", :class => "btn btn-primary btn-large" %>
	</div>
<% end %>
