<%= render 'shared/admin_title_options', :title => "Atualização de Usuário",
           :options => [ {:label => "Voltar", :path => admin_users_path, :type => "info"} ] %>

<%= form_for([:admin, @user], :html => {:class => "form-horizontal"}) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="controls">
    <%= f.submit "Salvar", :class => "btn btn-primary btn-large" %>
  </div>
<% end %>
