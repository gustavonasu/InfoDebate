<% forum_path = admin_forum_path(@forum_thread.forum) unless @forum_thread.forum.nil? %>
<%= render 'shared/admin/select2_js_template',
     :field_name => "forum_thread_forum_id",
     :label => "Forum",
     :obj => @forum_thread.forum,
     :index_path => admin_forums_path,
     :show_path => forum_path %>

<%= render 'shared/error_messages', :object => f.object %>
<%= render 'shared/field', :f => f, :field_name => :name %>
<%= render 'shared/field', :f => f, :field_name => :description %>
<%= render 'shared/field', :f => f, :field_name => :forum_id %>
