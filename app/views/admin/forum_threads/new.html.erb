<%= render 'shared/admin_title_options', :title => "Nova Thread",
 				:options => [ {:label => "Voltar", :path => admin_forum_threads_path, :type => "info"} ] %>

<%= form_for([:admin, @forum_thread], :html => {:class => "form-horizontal"}) do |f| %>
	<%= render 'fields', :f => f %>
	<div class="controls"> 
	    <%= f.submit "Salvar", :class => "btn btn-primary btn-large" %>
	</div>
<% end %>
